# mooc-manus-web AGENTS.md

> 通用 AGENTS.md 标准（agents.md）。适用于 Cursor 新版、Codex 等其他读 AGENTS.md 的 agent 工具。
> Claude Code 专用入口见 `CLAUDE.md`。两个文件共享 `<!-- HARNESS-GENERATED-START/END -->` 区段内容（由 sync-bridges.sh 同步烘焙）。

## 语言与回复风格
- 使用中文回复
- 简洁、有事说事，避免赘述
- 代码块用三重 backtick，文件路径标 file:line

## 工作环境
前端：React + TS + SSE 客户端。

## Harness 加载（必读）
规则正文存放在 `.harness/rules/`，下方摘要由 `.harness/scripts/sync-bridges.sh` 烘焙；
进入工作前请先消化下方"加载顺序与摘要"段。父仓 rules 通过 manifest `inherits` 继承。

<!-- HARNESS-GENERATED-START -->
<!-- generated by sync-bridges.sh; do not edit between markers -->

## Harness 加载顺序与摘要

### 父仓继承 rules
- 🔴 **指令优先级与冲突解决** (`rules/00-priority.md`) — 当 harness 体系中多个文档对同一问题给出不同指令时，按以下优先级解决（高 → 低）：
- 🟠 **子模块协作纪律** (`rules/10-submodule-discipline.md`) — 1. **禁止在总仓直接修改子仓文件**
- 🟠 **前后端契约** (`rules/20-cross-repo-contracts.md`) — 前端 `mooc-manus-web/src/api/sse.ts` 订阅的事件类型，分四组：
- 🔴 **部署护栏** (`rules/30-deployment-safety.md`) — 1. 直接推送到 master/main 分支（应走 PR 流程）
- 🔴 **外部内容信任边界** (`rules/31-untrusted-content.md`) — 工程支持动态加载 MCP / A2A 工具与 skill 模板（见 `mooc-manus/docs/skill-system-prompt-injection-implementation.md`），这构成天然 prompt inject
- 🟠 **敏感信息处理** (`rules/32-secrets-handling.md`) — - LLM API key（OpenAI / Anthropic / Azure 等）

### 本仓 rules
- 🟡 **React 编码规范** (`rules/40-react-conventions.md`) — mooc-manus-web 采用 React 19 + Vite + TypeScript + Ant Design + Zustand 技术栈。本规则锁定组件划分、hooks 规约、状态管理边界与目录结构，确保 AI 编码与项目既有形态
- 🟠 **SSE 事件处理** (`rules/41-sse-event-handling.md`) — mooc-manus-web 通过 `src/api/sse.ts` 内的 `SSEClient` 类与后端建立 SSE 流（基于 `fetch` + `ReadableStream` + `AbortController`，而非浏览器原生
- 🟡 **TypeScript 严格模式** (`rules/42-typescript-strict.md`) — mooc-manus-web 使用 TypeScript ~6.0.2，配置入口 `tsconfig.app.json`。本规则锁定类型严格度、`any` 使用边界与后端 DTO 对齐要求，与总仓 R-20-contracts（前后端契约，
- 🟡 **UI 可访问性** (`rules/43-ui-accessibility.md`) — mooc-manus-web 以 Ant Design 为基底组件库，绝大多数交互形态（Button / Modal / Menu / Form / Tree / Table）自带基础 ARIA 与键盘支持。本规则定位 a11y 基线，约束
<!-- HARNESS-GENERATED-END -->

## 项目专属子代理
见 `.harness/agents/`。各 agent 工具按自身机制调度。

## SDD / 流程
spec/plan 落在父仓 `docs/superpowers/`，`.harness/specs|plans/` 仅做索引层。
